import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.blank.starter',
  appName: 'cs-blank-starter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
